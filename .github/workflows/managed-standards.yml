name: Managed standards

on:
  pull_request:
  push:
    branches: [staging, release, main]

jobs:
  validate:
    uses: Interspace-Ventures/exo/.github/workflows/managed-repository.yml@7e08202dfe06abd7a4c8e3667ddf2f177ff8f2e8
    with:
      workflow-contract-version: "2"
      node-version: "24"
      package-manager: pnpm
      package-manager-version: "11.9.0"
      install-command: "pnpm install --frozen-lockfile"
      typecheck-command: "pnpm run typecheck"
      build-command: "pnpm railway:build"
